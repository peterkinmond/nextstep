%h3 Edit project name

= form_for(@project, :html => { :class => 'form-inline'}) do |f|
  = f.text_field :name, :class => "NewProject", :size => 100, :autofocus => true, :style => "height:20px;width:600px;"
  = f.submit "Update", :class => 'btn btn-primary'
