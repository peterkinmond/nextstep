%h3 Edit step
= form_for(@step) do |f|
	.field
		= f.text_area :content, :class => "newStep", :autofocus => true, :style => "height:16px;width:600px;"
		= f.select(:estimated_time, %w[5 10 15 20 25 30 35 40], :selected => 10)
		min
	.actions
		= f.submit "Update"